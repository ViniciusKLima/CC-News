import { Component } from '@angular/core';

// Página de histórico de edições arquivadas (rota /arquivo). Ainda não
// implementada: hoje só exibe um título fixo, sem consumir o EditionService.
@Component({
  selector: 'app-archive',
  imports: [],
  templateUrl: './archive.html',
  styleUrl: './archive.scss',
})
export class Archive {

}
